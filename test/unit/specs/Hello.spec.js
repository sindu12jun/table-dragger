import OriginTable from '../../../src/OriginTable';

describe('origintable', () => {
  beforeEach(() => {
    const table = document.createElement('table');
    table.innerHTML = '<tr>123</tr>'
    console.log(table);
  })
  it('should render correct contents', () => {
  });
});
